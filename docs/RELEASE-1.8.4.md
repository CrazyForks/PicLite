# PicLite 图轻 v1.8.4

- 修复批量重命名的自定义连接符不作用于 `{name}` 的问题；现在可统一替换文件名中的空格、横线和下划线等连接符。
- 将“智能首次优化”调整为持续生效的“智能压缩”，并可在悬浮窗左下角直接切换智能压缩与自定义压缩。
- 智能压缩始终按高质量参数比较 JPEG、WebP、PNG 的实际结果；自定义压缩继续使用设置页中保存的格式、质量和缩放参数。
- 修复悬浮窗“再缩小一半”生成新图片后体积和尺寸没有及时刷新的问题，并支持连续减半处理当前结果。
- 调整悬浮窗的展开结果与打开完整工作台按钮顺序和图标，使操作含义保持一致。
